# =============================================================================
# DEPRECATED — This file has been replaced by site.yml
# =============================================================================
# The monolithic playbook has been split into individual playbooks under
# the playbooks/ directory, orchestrated by site.yml.
#
# Full run:          ansible-playbook site.yml
# Individual stages: ansible-playbook playbooks/jenkins.yml
#
# This file is kept for backward compatibility only.
# =============================================================================
---

- import_playbook: site.yml
